import { MySearchPipePipe } from './my-search-pipe.pipe';

describe('MySearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MySearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
